syntax on

" for editing parameters.yml (or any file mounted in container)
set backupcopy=yes " for docker https://github.com/docker/docker/issues/15793#issuecomment-161162118

set tabstop=4

set shiftwidth=4    " Indents will have a width of 4

set softtabstop=4   " Sets the number of columns for a TAB

set expandtab       " Expand TABs to spaces

